# English Text Classification Toolkit (PyTorch)

An end-to-end NLP pipeline for English news topic classification built with PyTorch and transformer-based models.
The project implements custom training loops, evaluation metrics, and inference scripts without relying on high-level trainers.
